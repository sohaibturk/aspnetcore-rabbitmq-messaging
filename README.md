### .NET RabbitMQ Messaging
A simple ASP.NET Core Web API that sends messages to a RabbitMQ queue using `IRabbitMqConnection` and `IMessageProducer` interfaces. The messages are consumed by a separate **console application**, demonstrating a minimal producer-consumer setup with RabbitMQ.
